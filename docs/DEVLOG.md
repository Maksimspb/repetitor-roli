# Дневник разработки

Живой документ: состояние проекта, принятые решения, рефлексии, что делать дальше.
Читать первым при продолжении на другом компе.

---

## Текущее состояние (на момент выкладки в GitHub)

Работает end-to-end: выбор роли → фильтр её сцен с описанием → репетиция
(партнёр озвучивает, твои реплики скрыты) → распознавание речи с живой обратной
связью → пословная подсветка ошибок → сводка по сцене с процентом.

**Фазы 0, 1, 2 (базовая) — сделаны.** Дальше: Whisper вместо браузерного ASR, потом «умное» (Фаза 3).

### Что проверено
- Парсер на эталонной пьесе: 955 реплик, 20 ролей, 5 действий, 50 явлений, 183 ремарки, неизвестных ролей нет.
- Слияние вариантов имён (Анфуса/Анфиса → 52 реплики, Мурзавецкий+Аполон → 62).
- Фильтр сцен по роли (Лыняев — 15 явлений из 50).
- Алгоритм диффа: 100% при точном, 79% при 3 ошибках (подсвечивает именно их), 0% при мимо.
- JS проходит `node --check`, сервер отдаёт `HTTP 200` с `no-store`.

### Что ещё сыро / не проверено вживую
- **Голос металлический** — на машине разработки стоит только базовая Milena.
  Нужен скачанный **Milena (Enhanced/Premium)** (Siri-голоса браузеру недоступны!).
- **Браузерное распознавание** в Safari не протестировано под нагрузкой — может капризить.
  Рекомендация пользователю: если Safari не слышит — открыть в Chrome.
- Живой прогон полной сцены с реальным микрофоном ещё не делался пользователем до конца.

---

---

## Фаза 2.1 — навигация, прогресс, устойчивый микрофон

Задача от пользователя: выйти из сцены и вернуться на то же место; видеть в списке,
что не закончено; видеть лучший результат по попаданию в слова; **микрофон работает
неустойчиво — в одних сценах слышит, в других отваливается, особенно в наушниках**.

### Что сделано

**Навигация.** Выход из сцены стрелкой «‹» или `Esc` в любой момент; позиция и уже
сделанные реплики пишутся в `localStorage`. В списке сцен — метка `⏸ 5/14` (не закончено)
или `✓ рекорд 87%`. При возврате нижняя панель предлагает «Продолжить» / «Сначала»,
продолжение показывает две последние реплики для контекста. Наверху экрана настройки —
карточка «Продолжить» с последней брошенной сценой.

**Рекорды.** Два уровня: рекорд по сцене (средний % за **полный** проход — частичные
прогоны рекорд не портят) и рекорд по каждой реплике (`rr.linebest.v1`), который виден
прямо на реплике до того, как ты её скажешь, и в разборе после.

**Микрофон.** Причина срывов оказалась не одна:

| Что происходило | Почему | Что сделано |
|---|---|---|
| Распознавание молча умирало и не поднималось | `listening` ставился только в `onstart`; если `onstart` не приходил, `onend` не перезапускал | Менеджер `Mic` со сторожевым таймером на `onstart` (1.8 с) и перезапуском с нарастающей паузой |
| Новая сессия падала сразу после старта | `recog.start()` вызывался, пока прошлый движок ещё не отпустил микрофон (`InvalidStateError` глотался) | `release()` возвращает промис и ждёт реального `onend`; `engage()` всегда ждёт его |
| В наушниках отваливалось чаще | Микрофон поднимался сразу после звука партнёра; Bluetooth в этот момент переключает маршрут A2DP↔HFP | Микрофон **отпускается** на реплике партнёра и поднимается через паузу (320 мс), которая при повторных срывах растёт сама |
| Ошибки были невидимы | `onerror` обрабатывал только `not-allowed` | Все коды разведены на «штатные» (`no-speech`, `aborted` — молча поднять) и «диагноз» (`network`, `audio-capture`, `not-allowed`), состояние всегда видно чипом в панели |
| «Нет доступа» показывалось как «выкл» | `onerror` ставил диагноз, идущий следом `onend` затирал его | Терминальные состояния защищены от перезаписи |
| С телефона по Wi-Fi микрофона не было вообще | `http://<ip>:8777` — незащищённый контекст, браузер не даёт микрофон ни при каких настройках | Проверка `isSecureContext` с прямым объяснением; в README — «на телефоне открывай https» |
| Сцена намертво вставала на реплике партнёра | ни `ended` от mp3, ни `onend` от синтеза не приходили (известное поведение Safari) | Сторожевой таймер озвучки, привязанный к реальной длительности файла |

**Диагностика.** Экран «🎙 Проверить микрофон» — 7 шагов: защищённый контекст, движок,
разрешение, устройство записи (показывает, какой именно микрофон пишет), уровень сигнала
с индикатором, распознавание, и главный — **«голос партнёра → микрофон»**, который
воспроизводит реальную реплику и сразу слушает. Именно этот шаг ловит поломку с наушниками.
Чистый прогон возвращает разросшуюся паузу обратно к 320 мс.

**Эргономика.** Кнопка старта и все действия — в нижней панели под большим пальцем
(до этого до «Начать репетицию» надо было прокрутить список из 16 сцен). Роли —
компактные чипы вместо длинного списка; выбор роли и сцены запоминается. Полоса прогресса
и счётчик «твоих реплик 4 из 14» в шапке. «Действие 1 · явление 10» цифрами вместо
«Д.первое · явл. десятое». Регулятор размера текста, `wake lock` (экран не гаснет),
безопасные отступы под вырез и жест-бар iPhone, `prefers-reduced-motion`.

### Добивка: «слышит ровно через раз»

После выкладки пользователь описал устойчивый узор: реплику слышит, следующую — нет,
через одну — снова слышит. Такая чёткая периодичность бывает только у гонки с двумя
состояниями, и она нашлась.

`finish()` ждал конца сессии максимум 700 мс, после чего обнулял `this.rec` и считал
микрофон свободным. На репликах, где пользователь **действительно говорил**, движок
закрывается дольше — доезжает финальный результат из облака. Этих 700 мс не хватало:
приложение играло партнёра и поднимало новую сессию поверх ещё живой. Она заводилась,
чип честно писал «слушаю», но звук в неё не шёл. Следующая реплика оставалась пустой,
закрывалась мгновенно — и работала. Отсюда строгое чередование.

Починка: промис `dying` — «движок физически отпустил микрофон». Ни один путь запуска
не поднимает сессию, пока он не сработал; ожидание хвоста осталось коротким (700 мс,
столько терпит рука), а полное закрытие уехало в фон и успевает, пока говорит партнёр.
Добавлены: счётчик `turn` (отменяет повисший `engage`), сторож на «слушаю, но глухая»
сессию, самолечение (две пустые реплики подряд → пауза перед стартом растёт) и журнал
событий движка с кнопкой «скопировать» — потому что воспроизвести это можно только
на живом телефоне.

В тест добавлен подсчёт **наложений сессий**: подставной движок считает, сколько
распознавателей держат микрофон одновременно. Больше одного — это и есть баг.
На медленно закрывающемся движке (1.2 с) старый код давал наложения, новый — ноль.

### Фаза 2.3: Whisper в браузере — замерен и выключен

Логичный следующий шаг после серверного Whisper: гонять модель прямо на телефоне,
без сервера и интернета. Реализация была написана и выложена. **Замер показал, что
путь непригоден**, и это тот случай, когда цифры важнее правдоподобной идеи.

Проверялось честно: пред-рендеренные mp3 партнёра — это настоящая русская речь
с точно известным текстом, так что попадание считается тем же `wordDiff`, что и в
приложении.

| Связка | Результат |
|---|---|
| любая модель whisper + `dtype:'q8'` | **сессия не создаётся вообще**: `Missing required scale: model.decoder.embed_tokens.weight_merged_0_scale` |
| `onnx-community/whisper-base`, fp32-энкодер + q4-декодер | 63% попадания, 1.8× от реального времени |
| `onnx-community/whisper-tiny`, fp32 + q4 | 40% попадания, 0.6× |

Три вывода:

1. **Выложенная конфигурация не работала никогда.** Стоял `q8`, который не поднимается
   ни у одной модели. А `initASR()` переключался на этот движок безусловно, не проверив
   его работоспособность, — то есть на телефоне каждая реплика возвращала пустоту.
   Симптом «микрофон работает через раз» на телефоне после этого стал бы «не работает никогда».
2. **Даже рабочая связка бесполезна для этой задачи.** 63% — на студийной записи без шума,
   то есть потолок. Тренажёр меряет попадание в слова; счётчик, который показывает 63%,
   когда ты сказал идеально, обесценивает весь смысл. Правильный ресемплинг через
   `OfflineAudioContext` вместо линейной интерполяции не меняет ничего (63% против 63%) —
   упирается в модель, а не в обвязку.
3. **Скорость добивает.** 1.8× от реального времени на десктопе означает, что 10-секундная
   реплика распознаётся 18 секунд; на телефоне кратно дольше. Репетиция перестаёт быть репетицией.

Что сделано: движок остался в коде, но выключен и честно подписан замерами; выбирается
только вручную и только после того, как модель реально поднялась. Для точности на телефоне
добавлено поле адреса сервера распознавания — туда вписывается https-туннель к своему Маку,
и телефон получает ту же точность, что за столом, ничего не скачивая.

**Правило, которое из этого следует:** движок распознавания берётся, только когда доказал,
что живой. Любой отказ на ходу возвращает `Mic` на `MicWeb`. Раньше отказ означал молчаливые
нули до конца репетиции.

### Фаза 2.4: журнал с айфона закрыл вопрос

Всё предыдущее лечение микрофона строилось на догадках. Журнал с живого устройства
(iOS 18.7, Safari 26.5) занял три экрана и ответил сразу на всё.

```
113   web: engage → 113.9 start → 115.2 onstart+audiostart → 126 finish → ended:onend
135.8 web: engage → 136.7 start → 136.7 onstart+audiostart → 149.5 finish → ended:onerror
...
209.2 web: onstart+audiostart → 221.2 web: deaf     ← 11 секунд ни одного слова
пауза перед стартом: 1400 мс
движок: Whisper в браузере
```

Что из этого следует:

1. **Микрофон захватывается всегда.** `onstart` и `audiostart` есть в каждой сессии,
   ни одной ошибки за весь прогон. Значит доступ, разрешение и запись в порядке —
   вся линия рассуждений про «микрофон не дают» была мимо.
2. **Пауза перед стартом уже стояла в максимуме (1400 мс) и не помогла.** Теория про
   переключение аудиомаршрута Bluetooth не подтвердилась: сессии заводятся мгновенно
   и без ошибок. Хуже того, автоматическое наращивание паузы срабатывало на глухих
   сессиях, то есть лечило не ту болезнь и просто добавляло задержку.
3. **Первая сессия закрылась чисто (`ended:onend`), все последующие — через ошибку.**
   Классическая подпись iOS: Safari отдаёт распознавание надёжно только той сессии,
   которую начал жест пользователя. Первая реплика заводилась сразу после нажатия
   «Начать репетицию» и потому сработала; остальные стартовали программно после
   озвучки партнёра — заводились, но результатов не приносили.
4. **В шапке журнала — движок «Whisper в браузере».** Пользователь включил
   экспериментальный тумблер, и модель качалась в фоне **во время репетиции**.
   Браузерное распознавание на iPhone облачное; сотни мегабайт закачки съедали сеть,
   которая ему нужна. Вторая причина, наложившаяся на первую.

### Что сделано

- **`Mic.engageNow()`** — запуск строго внутри обработчика касания, без единого `await`.
  Важная тонкость: обычный `engage()` ждёт смерти прошлой сессии и паузу маршрута,
  и эти два `await` рвут связь с жестом. Для iOS нужен именно синхронный путь.
- **Режим «нажми и говори»**: на своей реплике кнопка сначала «🎤 Говорить»
  (она и стартует микрофон), потом «Дальше». Лишнее нажатие — зато слышит.
  Включается сам после **трёх** глухих подряд. Порог именно три, а не два: отличить
  «движок оглох» от «человек забыл текст» по потоку результатов невозможно,
  а три молчаливые реплики подряд у человека, который репетирует, маловероятны.
- **Диагнозы разведены.** Сессия не заводится → проблема аудиомаршрута → растить паузу.
  Сессия завелась, но не слышит ни слова → не хватает жеста → «нажми и говори».
  Раньше обе лечились наращиванием паузы, то есть одна из них не лечилась вовсе.
- Пауза сброшена к 320 мс: её раздуло неверным диагнозом.
- Журнал теперь пишет код ошибки (`ended:onerror:no-speech` против просто `onerror`),
  время до первого результата и число услышанных слов. Без этого пришлось бы гадать снова.
- Предупреждение при включении on-device Whisper: закачка мешает облачному распознаванию.

**Урок.** Полгода можно чинить микрофон по симптомам и каждый раз попадать мимо.
Один журнал с настоящего устройства — три экрана текста — закрыл вопрос за один заход.
Инструментировать надо было раньше, чем чинить.

### Как проверялось
Автотест `drive.mjs` (в репозиторий не входит) гоняет приложение в headless Chrome через
CDP с подставным распознавателем: 34 проверки — полный проход сцены, выход и возврат,
метки и рекорды, и отдельно поведение микрофона при отказе в доступе, при молчащем движке,
при рвущейся каждые 60 мс сессии и при мёртвой озвучке. Три бага (затирание диагноза,
слишком долгий таймаут, зависание на немой реплике) нашёл именно он, а не чтение кода.

---

## Фаза 2.2 — локальный Whisper вместо браузерного распознавания

Проблема пользователя: даже после героической стабилизации `Mic` (поколения, сторожевые
таймеры, обработка BT-маршрута) браузерный Web Speech **принципиально нестабилен** — одну
реплику слышит, три следующие нет, «зарабатываешь нули», иногда после перемоток оживает.
Это не баг в нашем коде — это природа Web Speech (облачный, засыпает, throttling).

### Решение
Локальный **faster-whisper** как сменный движок:
- `asr_server.py` — отдельный HTTP-сервер (:8788, Python 3.11-venv `.venv-asr`). Принимает
  запись реплики POST-ом, прогоняет Whisper (`small`, CPU, int8, `language=ru`), возвращает `{text}`.
  CORS открыт, чтобы приложение с :8777 могло стучаться.
- В `index.html` движок стал сменным: `let Mic = MicWeb` (браузерный, как было) →
  `initASR()` пингует `/asr/health`, и если бэкенд жив — `Mic = MicWhisper`. Та же поверхность
  методов (`engage/release/finish/available/text`), поэтому `step()`, `finalizeMe`, панель
  управления не тронуты. `MicWhisper` пишет через `MediaRecorder`, на `finish()` шлёт blob и
  ждёт текст; живая обратная связь «слышу тебя» — через `AnalyserNode` (пульс микрофона по
  уровню сигнала, а не по капризному распознаванию).
- `run.sh` поднимает оба сервера. `Python 3.14` не подошёл (нет колёс ctranslate2) — взяли 3.11.

### Проверено
Сквозной тест (озвучка edge-tts → POST → Whisper): «Вы только платье переменили…» — идеально;
«…что за фантазия гулять по росе…» — одна ошибка на трудном месте (~90%). Стабильно, без срывов.

### Где работает
Только на Маке (за столом — там и нужна проверка текста). Телефон/GitHub Pages: бэкенда нет →
тихий откат на браузерный движок; в машине проверка не нужна (режим «🚗 Без рук»).

### Рефлексия
Героическая инженерия вокруг принципиально ненадёжного API — тупик. Правильный ход был не
«ещё сильнее чинить Web Speech», а сменить сам движок. Сменяемость через единую поверхность
(`Mic` как `let`, переключаемый на живой бэкенд) окупилась: ни одна строка UI/логики хода
не изменилась. Урок: когда фундамент нестабилен, чини фундамент, а не симптомы над ним.

---

## Принятые решения (и почему)

| Решение | Выбор | Почему |
|---|---|---|
| Платформа | **Веб-апп** (Mac-сервер + браузер телефона по Wi-Fi) | Быстрее всего собрать, работает и на Маке, и в руке; звук идёт на устройство, что держишь |
| Порядок фаз | Сначала **партнёр**, потом проверка | Так сформулировал пользователь: «вначале просто фраза на фразу», проверка — «в фоне» |
| Голос (MVP) | **Системный** (Web Speech), апгрейд до Enhanced | Бесплатно, офлайн, без бэкенда. Пользователь выбрал бесплатный путь |
| Проверка (MVP) | **Браузерный** Web Speech ASR, не Whisper | Ноль бэкенда, мгновенная обратная связь. Whisper — следующий шаг за точностью |
| Стек | Один HTML + parse.py + http-сервер, **без фреймворков** | «Клонировал → запустил». Минимум зависимостей для продолжения на другом компе |
| Сервер | Свой `server.py` с `no-store` | Safari кешировал старый index.html → баг «нет списка сцен». No-store убил проблему |

---

## Рефлексии

- **Кеш — главный тихий враг локальной веб-разработки.** Симптом («приложение не работает,
  нет списка сцен, сразу пишет „вы молодец“») выглядел как логическая ошибка, а на деле сервер
  отдавал новый файл, а Safari показывал старый. Урок: сразу ставить `Cache-Control: no-store`
  на дев-сервер, не тратить время на догадки. Проверка «что реально отдаёт сервер» (`curl | grep`)
  быстро отсекла код от кеша.

- **Обратная связь = продукт.** Пользователь не понял, «слышит ли его комп». Функционально
  распознавание можно было добавить и молча, но именно **видимость** («🔴 слушаю…», живой
  транскрипт) превращает молчаливое ожидание в диалог. Обратная связь важнее точности на этом этапе.

- **Голос решает восприятие.** «Металлический» голос обесценивает весь тренажёр — партнёр
  должен звучать живо. Тонкость, которую легко упустить: **системный голос ≠ голос в браузере**.
  Siri-голоса, которые пользователь выбрал в настройках, веб-приложению недоступны — нужен
  именно скачанный Enhanced. Это неочевидно и стоило отдельного разбора со скриншотом.

- **Парсер честнее додумывания.** Соблазн был сгенерировать «красивые» описания сцен LLM-кой,
  но описания из реальных данных (участники + первая строка + счётчик реплик) — точны и не врут.
  Для незнакомой адаптации это важнее «литературности».

- **Данные вперёд UI.** Всё удобство (фильтр сцен, описания, «все мои сцены подряд») стало
  возможным, потому что парсер заранее посчитал `participants`/`per_role`/`preview` на каждую сцену.
  Стоит вложиться в структуру данных — интерфейс потом собирается почти сам.

---

- **Не бывает «одной причины» у плавающего бага.** «Микрофон работает неустойчиво» разложился
  на шесть независимых поломок: гонка при старте сессии, необработанные коды ошибок, затирание
  диагноза, переключение аудиомаршрута Bluetooth, незащищённый контекст по Wi-Fi и зависание
  озвучки. Починка любой одной оставила бы симптом «в иных сценах слышит, в иных нет».

- **Устойчивость надо тестировать поломками, а не прогоном.** Ручной прогон показывает, что
  «работает». Ценность дал подставной распознаватель, которому можно приказать молчать, рвать
  сессию каждые 60 мс или отказать в доступе. Все три реальных бага вылезли там.

- **Состояние микрофона — часть интерфейса, а не деталь реализации.** Пока приложение молчало
  о том, что распознавание умерло, пользователь считал виноватым себя или наушники. Чип
  с состоянием и экран диагностики стоят дешевле, чем ещё один заход в отладку вслепую.

## Что делать дальше (по приоритету)

1. **Живой прогон на iPhone с наушниками** — единственное, что нельзя проверить с машины
   разработки: реальный Bluetooth-маршрут. Прогнать «🎙 Проверить микрофон» на самом телефоне,
   шаг 7 покажет, хватает ли паузы в 320 мс.
2. **Голос поживее** — довести Enhanced; если мало, собрать **локальный Silero TTS** (бесплатно,
   офлайн, разный голос на персонажа) — потребуется маленький Python-бэкенд.
3. **Фаза 2+: локальный Whisper** — заменить браузерный ASR на faster-whisper + forced alignment.
   Точнее на русском, офлайн, приватно. Появляется бэкенд `POST /asr`. Тот же `wordDiff` переиспользуется.
4. **Фаза 3: LLM-судья перифраза** — «своими словами, но верно» ≠ ошибка. Ключ к театральной,
   а не буквальной проверке.
5. **Авто-передача хода** без кнопки (по паузе), **SRS** (реплики с низким % — чаще),
   **голос на персонажа**, аналитика прогресса по акту.

## Идеи на потом
- Экспорт «карточек связок» (реплика-зацепка → твой ответ) для повторения без приложения.
- Режим «только мои реплики подряд» — прогнать монологом.
- Загрузка своей пьесы через UI (сейчас — заменой `data/play.docx` + `parse.py`).
- Мобильная установка как PWA.
